const animal = ""

function myAnimal(animal) {
  animal = "dog"
  return animal
}

function yourAnimal(animal) {
  animal = "cat"
  return animal
}

function add2(n) {
  let two = 2
  return n + two

  // Feel free to move things around!
}